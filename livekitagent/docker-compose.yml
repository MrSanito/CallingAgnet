services:
  livekit-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: livekit-agent
    restart: unless-stopped
    env_file:
      - ./src/.env
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"